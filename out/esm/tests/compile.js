import { Test } from 'ts-toolbelt';
const { checks, check } = Test;
checks([
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
    check(),
]);
//# sourceMappingURL=compile.js.map